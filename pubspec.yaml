name: tap_the_ball
description: لعبة بسيطة لتعلم Flutter
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/ball.png
